function StatusBar() {
  return (
    <div className="status-bar">
      <span className="empire-name">No save loaded</span>
      <span className="connection-status disconnected">Disconnected</span>
    </div>
  )
}

export default StatusBar
